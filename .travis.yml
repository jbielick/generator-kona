language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kona' ]; then cd .. && eval "mv $currentfolder generator-kona" && cd generator-kona; fi

